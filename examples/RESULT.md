# Recorded synthetic workflow

Run `python examples/resume_demo.py` from the installed checkout.

[Complete captured output](result.txt) comes from executing the adjacent synthetic example on 2026-09-20. The example uses disposable local state and makes no Discord or model calls. Timestamps or generated local identities may differ on another run. The README explains which part of the workflow this proves and which integration remains outside it.
